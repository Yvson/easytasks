export default class MyStopwatch {
    constructor(date, time_counter, increment, status) {
        this.date = date;
        this.time_counter = time_counter;
        this.increment = increment;
        this.status = status;
    }
}