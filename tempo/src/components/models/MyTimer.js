export default class MyTimer {
    constructor(date, time_counter, decrement, status) {
        this.date = date;
        this.time_counter = time_counter;
        this.decrement = decrement;
        this.status = status;
    }
}